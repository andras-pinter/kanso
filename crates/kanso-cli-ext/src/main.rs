// Phase 5 will:
//   - read the local API port + session token from the kanso app data dir
//   - expose Copilot CLI tools backed by HTTP calls to kanso-api:
//       kanso_list, kanso_add, kanso_move, kanso_done, kanso_search, kanso_open
//   - speak the Copilot CLI extension protocol over stdio

fn main() {
    println!("kanso-cli-ext placeholder (Phase 5)");
}
